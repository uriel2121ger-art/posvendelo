name: POSVENDELO — Release Artifacts

# Dispara en push de tags con formato vX.Y.Z
on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

# Variables compartidas entre todos los jobs
# Control-plane expuesto via Cloudflare Tunnel en posvendelo.com
env:
  CP_URL: "https://posvendelo.com"
  GHCR_IMAGE: "ghcr.io/uriel2121ger-art/posvendelo"

# ─────────────────────────────────────────────────────────────────────────────
# JOB 1: Backend — imagen Docker al GHCR + registro en control-plane
# ─────────────────────────────────────────────────────────────────────────────
jobs:
  build-backend:
    name: "Backend — Docker → GHCR"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4


      - name: Login al GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extraer version del tag
        id: ver
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Build y push imagen Docker
        uses: docker/build-push-action@v6
        with:
          context: backend
          push: true
          tags: |
            ${{ env.GHCR_IMAGE }}:${{ steps.ver.outputs.VERSION }}
            ${{ env.GHCR_IMAGE }}:latest

      - name: Registrar backend en control-plane
        env:
          CP_RELEASES_TOKEN: ${{ secrets.CP_RELEASES_TOKEN }}
          VERSION: ${{ steps.ver.outputs.VERSION }}
          GHCR_IMAGE: ${{ env.GHCR_IMAGE }}
        run: |
          curl -sf --max-time 60 \
            -H "X-Release-Token: ${CP_RELEASES_TOKEN}" \
            -H "Content-Type: application/json" \
            -X POST "${CP_URL}/api/v1/releases/upload" \
            -d "{
              \"platform\": \"desktop\",
              \"artifact\": \"backend\",
              \"version\": \"${VERSION}\",
              \"channel\": \"stable\",
              \"target_ref\": \"${GHCR_IMAGE}:${VERSION}\",
              \"source\": \"github-actions\"
            }"

# ─────────────────────────────────────────────────────────────────────────────
# JOB 2: Frontend Linux — AppImage + .deb
# ─────────────────────────────────────────────────────────────────────────────
  build-frontend-linux:
    name: "Frontend Linux — AppImage + deb"
    runs-on: ubuntu-latest
    env:
      CSC_IDENTITY_AUTO_DISCOVERY: "false"

    steps:
      - name: Checkout
        uses: actions/checkout@v4


      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Instalar dependencias
        working-directory: frontend
        run: npm ci

      - name: Build Linux (AppImage + deb)
        working-directory: frontend
        run: npm run build:linux

      - name: Extraer version del tag
        id: ver
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Subir AppImage al control-plane
        env:
          CP_RELEASES_TOKEN: ${{ secrets.CP_RELEASES_TOKEN }}
          VERSION: ${{ steps.ver.outputs.VERSION }}
        run: |
          APPIMAGE=$(find frontend/dist -name "*.AppImage" | head -1)
          echo "AppImage encontrado: $APPIMAGE"
          curl -sf --max-time 300 \
            -H "X-Release-Token: ${CP_RELEASES_TOKEN}" \
            -F "platform=desktop" \
            -F "artifact=electron-linux" \
            -F "version=${VERSION}" \
            -F "channel=stable" \
            -F "file=@${APPIMAGE}" \
            "${CP_URL}/api/v1/releases/upload"

      - name: Subir .deb al control-plane
        env:
          CP_RELEASES_TOKEN: ${{ secrets.CP_RELEASES_TOKEN }}
          VERSION: ${{ steps.ver.outputs.VERSION }}
        run: |
          DEB=$(find frontend/dist -name "*.deb" | head -1)
          echo ".deb encontrado: $DEB"
          curl -sf --max-time 300 \
            -H "X-Release-Token: ${CP_RELEASES_TOKEN}" \
            -F "platform=desktop" \
            -F "artifact=electron-linux-deb" \
            -F "version=${VERSION}" \
            -F "channel=stable" \
            -F "file=@${DEB}" \
            "${CP_URL}/api/v1/releases/upload"

      - name: Guardar artifacts para GitHub Release
        uses: actions/upload-artifact@v4
        with:
          name: posvendelo-linux
          path: |
            frontend/dist/*.AppImage
            frontend/dist/*.deb

# ─────────────────────────────────────────────────────────────────────────────
# JOB 3: Frontend Windows — NSIS .exe
# ─────────────────────────────────────────────────────────────────────────────
  build-frontend-windows:
    name: "Frontend Windows — NSIS exe"
    runs-on: windows-latest
    env:
      CSC_IDENTITY_AUTO_DISCOVERY: "false"

    steps:
      - name: Checkout
        uses: actions/checkout@v4


      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Instalar dependencias
        working-directory: frontend
        run: npm ci

      - name: Build Windows (NSIS)
        working-directory: frontend
        run: npm run build:win

      - name: Extraer version del tag
        id: ver
        shell: bash
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Subir .exe al control-plane
        shell: bash
        env:
          CP_RELEASES_TOKEN: ${{ secrets.CP_RELEASES_TOKEN }}
          VERSION: ${{ steps.ver.outputs.VERSION }}
        run: |
          EXE=$(find frontend/dist -name "*setup.exe" -o -name "*.exe" | head -1)
          echo "Instalador encontrado: $EXE"
          curl -sf --max-time 300 \
            -H "X-Release-Token: ${CP_RELEASES_TOKEN}" \
            -F "platform=desktop" \
            -F "artifact=electron-windows" \
            -F "version=${VERSION}" \
            -F "channel=stable" \
            -F "file=@${EXE}" \
            "${CP_URL}/api/v1/releases/upload"

      - name: Guardar artifact para GitHub Release
        uses: actions/upload-artifact@v4
        with:
          name: posvendelo-windows
          path: |
            frontend/dist/*setup.exe
            frontend/dist/*.exe

# ─────────────────────────────────────────────────────────────────────────────
# JOB 4: Frontend Android — APK (cajero)
# ─────────────────────────────────────────────────────────────────────────────
  build-frontend-android:
    name: "Frontend Android — APK cajero"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4


      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Instalar dependencias
        working-directory: frontend
        run: npm ci

      - name: Extraer version del tag
        id: ver
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          # Convertir vX.Y.Z a codigo numerico (ej: 1.2.3 → 10203)
          IFS='.' read -r MAJOR MINOR PATCH <<< "$VERSION"
          VERSION_CODE=$(( MAJOR * 10000 + MINOR * 100 + PATCH ))
          echo "VERSION=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "VERSION_CODE=${VERSION_CODE}" >> "$GITHUB_OUTPUT"

      - name: Build web para Android
        working-directory: frontend
        run: npm run build:browser

      - name: Sincronizar Capacitor
        working-directory: frontend
        run: npx cap sync android

      - name: Build APK release
        working-directory: frontend/android
        env:
          APK_VERSION_NAME: ${{ steps.ver.outputs.VERSION }}
          APK_VERSION_CODE: ${{ steps.ver.outputs.VERSION_CODE }}
        run: ./gradlew assembleRelease

      - name: Subir APK al control-plane
        env:
          CP_RELEASES_TOKEN: ${{ secrets.CP_RELEASES_TOKEN }}
          VERSION: ${{ steps.ver.outputs.VERSION }}
        run: |
          APK="frontend/android/app/build/outputs/apk/release/app-release-unsigned.apk"
          echo "APK encontrado: $APK"
          curl -sf --max-time 300 \
            -H "X-Release-Token: ${CP_RELEASES_TOKEN}" \
            -F "platform=android" \
            -F "artifact=android-cajero" \
            -F "version=${VERSION}" \
            -F "channel=stable" \
            -F "file=@${APK}" \
            "${CP_URL}/api/v1/releases/upload"

      - name: Guardar APK para GitHub Release
        uses: actions/upload-artifact@v4
        with:
          name: posvendelo-cajero-apk
          path: frontend/android/app/build/outputs/apk/release/*.apk

# ─────────────────────────────────────────────────────────────────────────────
# JOB 5: Owner App Linux — AppImage + deb
# ─────────────────────────────────────────────────────────────────────────────
  build-owner-linux:
    name: "Owner App Linux — AppImage + deb"
    runs-on: ubuntu-latest
    env:
      CSC_IDENTITY_AUTO_DISCOVERY: "false"

    steps:
      - name: Checkout
        uses: actions/checkout@v4


      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: owner-app/package-lock.json

      - name: Instalar dependencias
        working-directory: owner-app
        run: npm ci

      - name: Build Linux (AppImage + deb)
        working-directory: owner-app
        run: npm run build:linux

      - name: Extraer version del tag
        id: ver
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Subir AppImage al control-plane
        env:
          CP_RELEASES_TOKEN: ${{ secrets.CP_RELEASES_TOKEN }}
          VERSION: ${{ steps.ver.outputs.VERSION }}
        run: |
          APPIMAGE=$(find owner-app/dist -name "*.AppImage" | head -1)
          echo "AppImage encontrado: $APPIMAGE"
          curl -sf --max-time 300 \
            -H "X-Release-Token: ${CP_RELEASES_TOKEN}" \
            -F "platform=desktop" \
            -F "artifact=owner-linux" \
            -F "version=${VERSION}" \
            -F "channel=stable" \
            -F "file=@${APPIMAGE}" \
            "${CP_URL}/api/v1/releases/upload"

      - name: Subir .deb al control-plane
        env:
          CP_RELEASES_TOKEN: ${{ secrets.CP_RELEASES_TOKEN }}
          VERSION: ${{ steps.ver.outputs.VERSION }}
        run: |
          DEB=$(find owner-app/dist -name "*.deb" | head -1)
          echo ".deb encontrado: $DEB"
          curl -sf --max-time 300 \
            -H "X-Release-Token: ${CP_RELEASES_TOKEN}" \
            -F "platform=desktop" \
            -F "artifact=owner-linux-deb" \
            -F "version=${VERSION}" \
            -F "channel=stable" \
            -F "file=@${DEB}" \
            "${CP_URL}/api/v1/releases/upload"

      - name: Guardar artifacts para GitHub Release
        uses: actions/upload-artifact@v4
        with:
          name: posvendelo-owner-linux
          path: |
            owner-app/dist/*.AppImage
            owner-app/dist/*.deb

# ─────────────────────────────────────────────────────────────────────────────
# JOB 6: Owner App Windows — NSIS .exe
# ─────────────────────────────────────────────────────────────────────────────
  build-owner-windows:
    name: "Owner App Windows — NSIS exe"
    runs-on: windows-latest
    env:
      CSC_IDENTITY_AUTO_DISCOVERY: "false"

    steps:
      - name: Checkout
        uses: actions/checkout@v4


      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: owner-app/package-lock.json

      - name: Instalar dependencias
        working-directory: owner-app
        run: npm ci

      - name: Build Windows (NSIS)
        working-directory: owner-app
        run: npm run build:win

      - name: Extraer version del tag
        id: ver
        shell: bash
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Subir .exe al control-plane
        shell: bash
        env:
          CP_RELEASES_TOKEN: ${{ secrets.CP_RELEASES_TOKEN }}
          VERSION: ${{ steps.ver.outputs.VERSION }}
        run: |
          EXE=$(find owner-app/dist -name "*setup.exe" -o -name "*.exe" | head -1)
          echo "Instalador encontrado: $EXE"
          curl -sf --max-time 300 \
            -H "X-Release-Token: ${CP_RELEASES_TOKEN}" \
            -F "platform=desktop" \
            -F "artifact=owner-windows" \
            -F "version=${VERSION}" \
            -F "channel=stable" \
            -F "file=@${EXE}" \
            "${CP_URL}/api/v1/releases/upload"

      - name: Guardar artifact para GitHub Release
        uses: actions/upload-artifact@v4
        with:
          name: posvendelo-owner-windows
          path: |
            owner-app/dist/*setup.exe
            owner-app/dist/*.exe

# ─────────────────────────────────────────────────────────────────────────────
# JOB 7: Owner App Android — APK
# ─────────────────────────────────────────────────────────────────────────────
  build-owner-android:
    name: "Owner App Android — APK"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4


      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Node 22
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: owner-app/package-lock.json

      - name: Instalar dependencias
        working-directory: owner-app
        run: npm ci

      - name: Extraer version del tag
        id: ver
        run: |
          VERSION=${GITHUB_REF_NAME#v}
          IFS='.' read -r MAJOR MINOR PATCH <<< "$VERSION"
          VERSION_CODE=$(( MAJOR * 10000 + MINOR * 100 + PATCH ))
          echo "VERSION=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "VERSION_CODE=${VERSION_CODE}" >> "$GITHUB_OUTPUT"

      - name: Build web para Android
        working-directory: owner-app
        run: npm run build:web:android

      - name: Sincronizar Capacitor
        working-directory: owner-app
        run: npx cap sync android

      - name: Build APK release
        working-directory: owner-app/android
        env:
          APK_VERSION_NAME: ${{ steps.ver.outputs.VERSION }}
          APK_VERSION_CODE: ${{ steps.ver.outputs.VERSION_CODE }}
        run: ./gradlew assembleRelease

      - name: Subir APK al control-plane
        env:
          CP_RELEASES_TOKEN: ${{ secrets.CP_RELEASES_TOKEN }}
          VERSION: ${{ steps.ver.outputs.VERSION }}
        run: |
          APK="owner-app/android/app/build/outputs/apk/release/app-release-unsigned.apk"
          echo "APK encontrado: $APK"
          curl -sf --max-time 300 \
            -H "X-Release-Token: ${CP_RELEASES_TOKEN}" \
            -F "platform=android" \
            -F "artifact=owner-android" \
            -F "version=${VERSION}" \
            -F "channel=stable" \
            -F "file=@${APK}" \
            "${CP_URL}/api/v1/releases/upload"

      - name: Guardar APK para GitHub Release
        uses: actions/upload-artifact@v4
        with:
          name: posvendelo-owner-apk
          path: owner-app/android/app/build/outputs/apk/release/*.apk
        if: hashFiles('owner-app/android/gradlew') != ''

# ─────────────────────────────────────────────────────────────────────────────
# JOB FINAL: GitHub Release con todos los artifacts + checksums
# ─────────────────────────────────────────────────────────────────────────────
  publish-github-release:
    name: "Publicar GitHub Release"
    if: startsWith(github.ref, 'refs/tags/v')
    needs:
      - build-backend
      - build-frontend-linux
      - build-frontend-windows
      - build-frontend-android
      - build-owner-linux
      - build-owner-windows
      - build-owner-android
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Descargar artifacts Linux frontend
        uses: actions/download-artifact@v4
        with:
          name: posvendelo-linux
          path: release-artifacts/linux

      - name: Descargar artifacts Windows frontend
        uses: actions/download-artifact@v4
        with:
          name: posvendelo-windows
          path: release-artifacts/windows

      - name: Descargar APK cajero
        uses: actions/download-artifact@v4
        with:
          name: posvendelo-cajero-apk
          path: release-artifacts/android-cajero

      - name: Descargar artifacts Linux owner
        uses: actions/download-artifact@v4
        with:
          name: posvendelo-owner-linux
          path: release-artifacts/owner-linux

      - name: Descargar artifacts Windows owner
        uses: actions/download-artifact@v4
        with:
          name: posvendelo-owner-windows
          path: release-artifacts/owner-windows

      - name: Descargar APK owner
        uses: actions/download-artifact@v4
        with:
          name: posvendelo-owner-apk
          path: release-artifacts/owner-android
        continue-on-error: true

      - name: Generar checksums SHA256
        run: |
          cd release-artifacts
          find . -type f \( -name "*.AppImage" -o -name "*.deb" -o -name "*setup.exe" -o -name "*.exe" -o -name "*.apk" \) \
            -print0 | sort -z | xargs -0 sha256sum > SHA256SUMS.txt
          echo "=== SHA256SUMS ==="
          cat SHA256SUMS.txt

      - name: Publicar GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            release-artifacts/linux/*.AppImage
            release-artifacts/linux/*.deb
            release-artifacts/windows/*setup.exe
            release-artifacts/windows/*.exe
            release-artifacts/android-cajero/*.apk
            release-artifacts/owner-linux/*.AppImage
            release-artifacts/owner-linux/*.deb
            release-artifacts/owner-windows/*setup.exe
            release-artifacts/owner-windows/*.exe
            release-artifacts/owner-android/*.apk
            release-artifacts/SHA256SUMS.txt
